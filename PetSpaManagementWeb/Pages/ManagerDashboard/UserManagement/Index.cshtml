@page
@model PetSpaManagementWeb.Pages.ManagerDashboard.UserManagement.IndexModel
@{
    ViewData["Title"] = "Index";
}
<h1>Appointment</h1>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var customer in Model.Customers)
        {
            <tr>
                <td>@customer.Name</td>
                <td>@customer.Email</td>
                <td>
                    <a asp-page="./Details" asp-route-id="@customer.Id">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>
